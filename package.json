{
  "name": "blockchain-exchange-systems",
  "version": "1.0.0",
  "description": "Solidity smart contracts for an oracle-backed stablecoin exchange system.",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run scripts/deployExchange.js --network localhost",
    "deploy:sepolia": "hardhat run scripts/deployExchange.js --network sepolia"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.2.3",
    "@openzeppelin/contracts": "^5.0.2",
    "@chainlink/contracts": "^1.2.0",
    "dotenv": "^16.4.5",
    "ethers": "^5.7.2",
    "hardhat": "^2.22.19"
  }
}
